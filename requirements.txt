# Python Standard Library Only (No External Dependencies for Phase 1-2)
# This project intentionally uses only Python standard library to demonstrate
# understanding of core algorithms without relying on heavy frameworks.

# Python >= 3.10 required for:
# - Union type hints (X | Y)
# - Pattern matching (match/case) - if used later
# - Improved error messages

# === PHASE 3: Machine Learning Dependencies ===
# Explainable ML models only (Logistic Regression, Random Forest, GBM)
scikit-learn>=1.3.0
numpy>=1.24.0
joblib>=1.3.0
pandas>=2.0.0
xgboost>=2.0.0

# For model explainability
shap>=0.43.0

# For visualization (optional but recommended)
matplotlib>=3.7.0
seaborn>=0.12.0

# Current dependencies: NONE (standard library only)
# This demonstrates core programming skills without framework dependency.
